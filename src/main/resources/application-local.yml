spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/api_invocationsdb
    username: api_invocations
    password: api_invocations
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  
server:
  port: 8081
